import { GAME_NAME } from '@school-days/shared';

// Phase 3 implements the authoritative Socket.IO room server here:
// room codes, lobby/ready/start, engine.reduce() on validated actions, and
// per-player redacted state broadcasts. For now this is just a smoke check that
// the workspace wiring (shared import + tsx) is working.
console.log(`${GAME_NAME} server — scaffold only. Real server lands in Phase 3.`);
